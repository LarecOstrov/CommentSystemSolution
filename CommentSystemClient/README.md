## CommentSystemClient (Angular)
Frontend project for Comment System built with Angular.
Located in `CommentSystemClient/`.

### How to run
1. `cd CommentSystemClient`
2. `npm install`
3. `ng serve --open`